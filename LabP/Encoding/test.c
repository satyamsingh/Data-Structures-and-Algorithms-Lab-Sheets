#include<stdlib.h>
#include<stdio.h>
#include"BinaryEncode.h"
int main(){
/*
   Element a = (Element)malloc(sizeof(struct element));
    a->value = (char*)malloc(sizeof(char)*10);
    a->value = "100";
    a->action = (char*)malloc(sizeof(char)*10);
    Bin f= createBin(1);
    f = addBin(f,a);
*/

}
